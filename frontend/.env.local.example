# App environment

# FastAPI base URL (HTTP) for backend API
NEXT_PUBLIC_API_BASE=http://localhost:8000

# WebSocket URL for chat
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/chat

# AlphaEarth tiles (choose one approach)
# Option 1: single template URL containing {year}
# e.g., http://localhost:8080/alphaearth/{year}/{z}/{x}/{y}.png
# You can also set headers via a tile proxy if needed.
NEXT_PUBLIC_ALPHAEARTH_TILE_TEMPLATE=

# Option 2: explicit latest/previous URLs (no {year} placeholder)
# NEXT_PUBLIC_ALPHAEARTH_LATEST_URL=
# NEXT_PUBLIC_ALPHAEARTH_PREVIOUS_URL=
